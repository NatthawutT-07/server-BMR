# ============================================
# Docker Environment Variables
# Copy this to .env and update values
# ============================================

# PostgreSQL
POSTGRES_USER=bmr
POSTGRES_PASSWORD=bmr_secure_password_2024
POSTGRES_DB=bmr_db

# JWT (generate new: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_jwt_secret_here_change_this_in_production

# Server
NODE_ENV=production
PORT=5001

# Cloudflare Tunnel (optional)
# CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
